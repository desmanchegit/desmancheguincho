kind = "web"
previewPath = "/"
title = "Central dos Desmanches"
version = "1.0.0"
id = "artifacts/central-desmanches"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23183

[services.development]
run = "pnpm --filter @workspace/central-desmanches run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/central-desmanches", "run", "build" ]
publicDir = "artifacts/central-desmanches/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23183"
BASE_PATH = "/"
